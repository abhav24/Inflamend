-- Supabase CLI loads this file with `supabase db reset`.
-- The demo user and local sample data live in migrations/0003_seed_dev_data.sql
-- so schema and seed checkpoints remain ordered and reviewable.

select 'Inflamend seed data is applied by migration 0003_seed_dev_data.sql' as note;
